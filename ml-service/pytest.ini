[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# statsmodels is chatty about convergence on short synthetic series; the code
# handles fit failures explicitly, so these are noise rather than signal.
filterwarnings =
    ignore::UserWarning
    ignore::RuntimeWarning
    ignore::DeprecationWarning
